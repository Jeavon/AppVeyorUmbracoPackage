# version format
version: 1.0.0.{build}

# UMBRACO_PACKAGE_SUFFIX will only be used for Release builds
# example UMBRACO_PACKAGE_SUFFIX=beta
install:
  - cmd: npm install -g grunt-cli
  - cmd: set UMBRACO_PACKAGE_SUFFIX=
  - cmd: set UMBRACO_PACKAGE_MIN_VERSION=7.1.5
  - cmd: cd BuildPackage
  - cmd: Build.bat

# to disable automatic builds
build: off

artifacts:
  - path: BuildPackage\Package\*.nupkg
  - path: BuildPackage\Package\*.zip